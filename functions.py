# We can declare functions in Python using Def

# one function to complete an action
import math


def add_two_six(x):
    result = x + 2.6
    print(result)

# Create a variable and parameter(7)
amount = add_two_six(7)

# another function
def add_four_one(x):
    result = (x + 4.1)/5.5
    print(math.floor(result))

equal = add_four_one(9)